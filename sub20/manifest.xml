<package>
  <description brief="ROS driver for the SUB-20 device from XDIMAX.">

     The driver enables communication with a XDIMAX SUB-20 device via USB. The SUB-20 sources are downloaded from XDIMAX during build.

  </description>
  <author>Sebastian Haug</author>
  <license>LGPL</license>
  <review status="unreviewed" notes=""/>
  <url>http://ros.org/wiki/sub20</url>

  <rosdep name="libusb-1.0"/>
  
  <export>
    <cpp cflags="-I${prefix}/src" lflags="-Wl,-rpath,${prefix}/lib -L${prefix}/lib -lsub20"/>
  </export>

</package>
